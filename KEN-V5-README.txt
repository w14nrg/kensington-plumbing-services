KENSINGTON KEN V5 — REAL WEBGL VERSION

This replaces the photo/sprite viewer completely.

Upload these files to the ROOT of GitHub:
- ken.html
- ken.css
- ken.js
- ken-3d.js

Keep:
- _worker.js
- ken-schema.sql

V5 uses the same real browser-rendered 3D/WebGL approach as the Chelsea-style hologram:
- real Three.js geometry
- real OrbitControls
- full 360-degree orbit
- real scroll zoom
- real pan
- slow auto-rotation when idle
- smooth stylised 3D Ken
- dark hair and beard
- navy KPS clothing
- tool belt and wrench
- blinking and breathing
- automatic occasional wave
- automatic wrench lift
- small version of the same live 3D Ken in the chat header

There are no action buttons and no image-frame rotation.

The existing Ken preview chat still works on GitHub Pages.
Once Cloudflare is connected, the same UI uses the live 235-job backend, appointments and SumUp flow.
