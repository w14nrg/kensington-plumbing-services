import {createKenScene} from "/ken-3d.js";
const PAGE_MODE=!!document.getElementById("ken-page-app");const STORAGE_KEY="kps_ken_final_state";const defaults={sessionId:`session_${crypto.randomUUID()}`,conversation:[],serverState:{},estimate:null,reservation:null,leadId:null};let state={...defaults};try{const s=JSON.parse(localStorage.getItem(STORAGE_KEY)||"null");if(s&&typeof s==='object')state={...defaults,...s,sessionId:s.sessionId||defaults.sessionId}}catch{}const persist=()=>localStorage.setItem(STORAGE_KEY,JSON.stringify(state));persist();const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));const isHome=()=>location.pathname==='/'||/\/index\.html$/i.test(location.pathname);let main3d=null,mini3d=null,launcher3d=null,home3d=null;
function chatMarkup(full=false){return `<header class="ken-head"><div class="ken-chat-3d" data-ken-mini-3d></div><div class="ken-head-copy"><strong>Ken</strong><span>Plumbing assistant · Estimates · Booking</span>${full?'':'<a class="ken-full-link" href="/ken.html">Open full Ken estimator →</a>'}</div><div class="ken-online">Online</div>${full?'':'<button class="ken-close" type="button">×</button>'}</header><div class="ken-status"><span></span></div><div class="ken-messages" aria-live="polite"></div><div class="ken-mini-actions"></div><div class="ken-inputbar"><input maxlength="700" autocomplete="off" placeholder="Describe your plumbing problem…"><button class="ken-send" type="button">➜</button></div><div class="ken-footnote">Ken provides an estimated range from the information supplied. The exact price is confirmed on site before additional work proceeds.</div>`}
function popupMarkup(){return `<div class="ken-widget"><button class="ken-launcher" type="button"><div class="ken-launcher-3d" data-ken-launcher-3d></div><span><strong>Ask Ken</strong><small>Estimate & book a plumber</small></span></button><section class="ken-panel">${chatMarkup(false)}</section></div>`}
function homeMarkup(){return `<section id="ken-home-entry"><div class="ken-home-inner"><div><div class="ken-home-kicker">ASK KEN · PLUMBING ESTIMATOR</div><h2 class="ken-home-title">What can Ken help you with?</h2><p class="ken-home-sub">Describe the problem. Ken will talk it through, estimate the likely repair and show available appointments.</p><form class="ken-home-form" id="ken-home-form"><input id="ken-home-input" placeholder="e.g. My toilet keeps filling after I flush…"><button>Ask Ken →</button></form><div class="ken-home-trust"><span>Natural conversation</span><span>Live estimate</span><span>Online booking</span></div></div><div class="ken-home-3d" data-ken-home-3d></div></div></section>`}
function injectHome(){if(!isHome()||document.getElementById('ken-home-entry'))return;const h=document.createElement('div');h.innerHTML=homeMarkup();const el=h.firstElementChild;const header=document.querySelector('header'),main=document.querySelector('main');if(header)header.insertAdjacentElement('afterend',el);else if(main?.parentNode)main.parentNode.insertBefore(el,main);else document.body.prepend(el);el.querySelector('#ken-home-form')?.addEventListener('submit',e=>{e.preventDefault();const i=el.querySelector('#ken-home-input'),t=i.value.trim();if(!t)return i.focus();i.value='';openKen();sendUserMessage(t)});home3d=createKenScene(el.querySelector('[data-ken-home-3d]'),{mini:true,room:false})}
function root(){return PAGE_MODE?document.getElementById('ken-page-app'):document.querySelector('.ken-panel')}function q(s){return root()?.querySelector(s)}function box(){return q('.ken-messages')}
function renderMessage(text,role='bot',save=true){const b=box();if(!b)return;const row=document.createElement('div');row.className=`ken-row ${role}`;row.innerHTML=role==='bot'?`<div class="ken-msg-avatar">K</div><div class="ken-bubble">${esc(text).replace(/\n/g,'<br>')}</div>`:`<div class="ken-bubble">${esc(text).replace(/\n/g,'<br>')}</div>`;b.appendChild(row);b.scrollTop=b.scrollHeight;if(save){state.conversation.push({role:role==='user'?'user':'assistant',content:text});state.conversation=state.conversation.slice(-24);persist()}if(role==='bot')mini3d?.setMood('speaking')}
function addHtml(html){const b=box();if(!b)return;const w=document.createElement('div');w.innerHTML=html;while(w.firstChild)b.appendChild(w.firstChild);b.scrollTop=b.scrollHeight}function setQuick(items=[]){const bar=q('.ken-mini-actions');if(!bar)return;bar.innerHTML='';items.forEach(text=>{const btn=document.createElement('button');btn.className='ken-mini-action';btn.textContent=text;btn.onclick=()=>sendUserMessage(text);bar.appendChild(btn)})}function progress(n){const el=q('.ken-status span');if(el)el.style.width=`${Math.max(12,Math.min(100,n||18))}%`}function typing(on){q('[data-typing]')?.remove();if(!on)return;const t=document.createElement('div');t.className='ken-typing';t.dataset.typing='1';t.textContent='Ken is thinking…';box()?.appendChild(t);mini3d?.setMood('thinking')}
async function api(path,options={}){const r=await fetch(path,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});let d={};try{d=await r.json()}catch{}if(!r.ok){const e=new Error(d.error||'Service unavailable');e.status=r.status;e.data=d;throw e}return d}
function localFallback(message){const m=message.toLowerCase(),s=state.serverState||{};if(/smell gas|gas leak/.test(m))return{reply:'Please leave the area, avoid operating electrical switches and call the National Gas Emergency Service on 0800 111 999.',safety:'Suspected gas leak: leave the area and call 0800 111 999.',state:s,progress:20};if(!s.demoType){if(/toilet|cistern|wc/.test(m))s.demoType='toilet';else if(/leak|pipe|water/.test(m))s.demoType='leak';else if(/tap|mixer/.test(m))s.demoType='tap';else if(/shower|bath/.test(m))s.demoType='shower';else if(/radiator|heating/.test(m))s.demoType='heating';else if(/blocked|drain|sink/.test(m))s.demoType='drain';else s.demoType='general';return{reply:s.demoType==='toilet'?'Is water continuously running into the toilet bowl, or is the cistern mainly taking a long time to refill?':s.demoType==='leak'?'Is water actively escaping now, and can you see exactly where it is coming from?':'Tell me a little more about what is happening and whether the affected part is easy to get to.',state:s,progress:38,quickReplies:s.demoType==='toilet'?['Running into the bowl','Slow to refill','Not flushing','Leaking at the base']:[]}}if(!s.demoAccess){s.demoAccess='known';return{reply:'Thanks. What is the postcode for the job?',state:s,progress:62}}if(!s.postcode){const pc=message.toUpperCase().match(/\b[A-Z]{1,2}\d[A-Z\d]?\s*\d[A-Z]{2}\b/);if(pc){s.postcode=pc[0];const map={toilet:['Toilet cistern / flush valve repair',95,190],leak:['Accessible leak repair / diagnosis',95,225],tap:['Tap repair or replacement',95,245],shower:['Shower fault diagnosis / repair',125,325],heating:['Radiator or heating-side diagnosis',75,275],drain:['Local waste / blockage clearance',95,285],general:['Plumbing fault diagnosis',75,220]};const x=map[s.demoType]||map.general;return{reply:'Thanks — that gives me enough to put together an initial estimate.',state:s,progress:100,estimate:{estimateId:`demo_${crypto.randomUUID()}`,jobName:x[0],min:x[1],max:x[2],confidence:'Medium',summary:'Preview estimate until the live Ken service is connected.'},demo:true}}return{reply:'What is the postcode for the job? For example, W14 9BP.',state:s,progress:70}}return{reply:'I have enough information for an estimate.',state:s,progress:100}}
function openKen(){if(PAGE_MODE)return;document.querySelector('.ken-panel')?.classList.add('open');setTimeout(()=>q('.ken-inputbar input')?.focus(),60)}function closeKen(){document.querySelector('.ken-panel')?.classList.remove('open')}
async function sendUserMessage(text){const message=String(text||'').trim();if(!message)return;renderMessage(message,'user');setQuick([]);typing(true);try{const d=await api('/api/ken',{method:'POST',body:JSON.stringify({sessionId:state.sessionId,message,history:state.conversation.slice(0,-1),state:state.serverState,page:location.pathname})});typing(false);state.sessionId=d.sessionId||state.sessionId;state.serverState=d.state||state.serverState;persist();progress(d.progress||35);if(d.safety)addHtml(`<div class="ken-safety">${esc(d.safety)}</div>`);if(d.reply)renderMessage(d.reply,'bot');if(d.quickReplies?.length)setQuick(d.quickReplies);if(d.estimate){state.estimate=d.estimate;persist();showEstimate(d.estimate);progress(78)}}catch(e){typing(false);const d=localFallback(message);state.serverState=d.state||state.serverState;persist();if(d.safety)addHtml(`<div class="ken-safety">${esc(d.safety)}</div>`);renderMessage(d.reply,'bot');if(d.quickReplies?.length)setQuick(d.quickReplies);if(d.estimate){state.estimate=d.estimate;persist();addHtml('<div class="ken-demo-note">Preview mode: the live conversation, appointments and payment switch on when the Cloudflare backend is connected.</div>');showEstimate(d.estimate);progress(78)}else progress(d.progress||35)}}
function showEstimate(e){if(q(`[data-estimate="${CSS.escape(e.estimateId)}"]`))return;addHtml(`<div class="ken-estimate" data-estimate="${esc(e.estimateId)}"><div class="ken-estimate-kicker">YOUR ESTIMATED REPAIR</div><h3>${esc(e.jobName)}</h3><div class="ken-price">£${esc(e.min)}–£${esc(e.max)}</div><p>${esc(e.summary||'Based on what you have told Ken so far.')}</p><span class="ken-confidence">Estimate confidence: ${esc(e.confidence)}</span><div class="ken-pay-note"><strong>Ready to book?</strong><br>Choose an available appointment. The £75 payment covers attendance and diagnosis and is deducted from the final repair price when we carry out the work.</div><button class="ken-btn dark" type="button" data-load-slots style="width:100%;margin-top:10px">See available appointments</button></div>`);q('[data-load-slots]')?.addEventListener('click',loadSlots);mini3d?.setMood('success');main3d?.setMood('success')}
async function loadSlots(){const btn=q('[data-load-slots]');if(btn){btn.disabled=true;btn.textContent='Checking appointments…'}try{const d=await api('/api/slots');addHtml(`<div class="ken-slot-wrap"><div class="ken-slot-title">Choose an available appointment</div><div class="ken-slots">${d.slots.slice(0,10).map(s=>`<button class="ken-slot" data-slot="${esc(s.slotKey)}"><strong>${esc(s.dayLabel)}</strong><small>${esc(s.timeLabel)}</small></button>`).join('')}</div></div>`);q('.ken-slots')?.querySelectorAll('[data-slot]').forEach(b=>b.addEventListener('click',()=>reserveSlot(b.dataset.slot)))}catch(e){renderMessage('Live appointment slots switch on when Ken is connected through Cloudflare. The visual booking flow is ready.','bot')}finally{if(btn){btn.disabled=false;btn.textContent='See available appointments'}}}
async function reserveSlot(slotKey){try{const d=await api('/api/reserve-slot',{method:'POST',body:JSON.stringify({slotKey,sessionId:state.sessionId,estimateId:state.estimate?.estimateId})});state.reservation=d.reservation;persist();renderMessage(`Great — I’ve held ${d.reservation.displayLabel} for you for 35 minutes while you complete the booking.`,'bot');showLeadForm()}catch(e){renderMessage(e.message||'That appointment is no longer available. Please choose another.','bot')}}
function showLeadForm(){if(q('[data-ken-lead]'))return;addHtml(`<div class="ken-estimate"><div class="ken-estimate-kicker">CONFIRM YOUR DETAILS</div><h3>${esc(state.reservation?.displayLabel||'Your appointment')}</h3><form class="ken-lead-form" data-ken-lead><input name="name" placeholder="Your name" required><input name="phone" placeholder="Mobile number" required><input name="email" type="email" placeholder="Email (optional)"><button class="ken-btn primary" type="submit">Pay £75 with SumUp & confirm booking</button></form></div>`);q('[data-ken-lead]')?.addEventListener('submit',startCheckout)}
async function startCheckout(e){e.preventDefault();const form=e.currentTarget,button=form.querySelector('button'),v=Object.fromEntries(new FormData(form).entries());button.disabled=true;button.textContent='Preparing secure SumUp payment…';try{const lead=await api('/api/lead',{method:'POST',body:JSON.stringify({...v,sessionId:state.sessionId,estimateId:state.estimate?.estimateId,postcode:state.serverState?.postcode||'',reservationId:state.reservation?.reservationId})});const checkout=await api('/api/checkout',{method:'POST',body:JSON.stringify({leadId:lead.leadId,estimateId:state.estimate?.estimateId,reservationId:state.reservation?.reservationId})});location.href=checkout.checkoutUrl}catch(err){button.disabled=false;button.textContent='Pay £75 with SumUp & confirm booking';renderMessage(err.data?.setupRequired?'The SumUp connection is the final backend step. Once connected, this button takes the £75 payment and confirms the selected appointment.':(err.message||'I could not start the payment.'),'bot')}}
function submitInput(){const i=q('.ken-inputbar input'),t=i?.value.trim();if(!t)return;i.value='';sendUserMessage(t)}function boot(){if(state.conversation.length){state.conversation.forEach(m=>renderMessage(m.content,m.role==='user'?'user':'bot',false));if(state.estimate)showEstimate(state.estimate);if(state.reservation)showLeadForm()}else{renderMessage('Hi, I’m Ken. Tell me what’s gone wrong in your own words and I’ll help work out the likely problem, give you an estimated price range and help you book a plumber.','bot');setQuick(['I have a leak','My toilet has a problem','I have a tap or shower problem','I have a radiator or heating problem','I have a blocked sink or drain'])}}
function removeTawk(){document.querySelectorAll('iframe[src*="tawk.to"],iframe[src*="tawk"],.tawk-min-container,#tawkchat-container,.tawk-button-circle').forEach(el=>el.remove())}
function init(){removeTawk();try{new MutationObserver(removeTawk).observe(document.documentElement,{childList:true,subtree:true})}catch{}if(PAGE_MODE){document.getElementById('ken-page-app').innerHTML=chatMarkup(true);main3d=createKenScene(document.getElementById('ken-main-3d'),{mini:false,room:true})}else{injectHome();const h=document.createElement('div');h.innerHTML=popupMarkup();document.body.appendChild(h.firstElementChild);document.querySelector('.ken-launcher')?.addEventListener('click',openKen);document.querySelector('.ken-close')?.addEventListener('click',closeKen);launcher3d=createKenScene(document.querySelector('[data-ken-launcher-3d]'),{mini:true,room:false})}mini3d=createKenScene(q('[data-ken-mini-3d]'),{mini:true,room:false});q('.ken-send')?.addEventListener('click',submitInput);q('.ken-inputbar input')?.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();submitInput()}});document.querySelectorAll('[data-ken-prompt]').forEach(b=>b.addEventListener('click',()=>sendUserMessage(b.dataset.kenPrompt)));document.querySelectorAll('[data-ken-3d-action]').forEach(b=>b.addEventListener('click',()=>main3d?.play(b.dataset.ken3dAction)));document.querySelectorAll('[data-ken-view]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-ken-view]').forEach(x=>x.classList.remove('active'));b.classList.add('active');main3d?.setView(b.dataset.kenView)}));document.getElementById('ken-fullscreen')?.addEventListener('click',()=>main3d?.fullscreen());boot()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();